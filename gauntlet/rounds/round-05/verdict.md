# Round 5 — orchestrator verdict

- Tier A (machine): 40/40 · Tier B (council): 31/35 · Tier C (council): 20/25
- Raw total: **91** · after caps: **91**
- Caps applied: none
- Red flags: "onboarding-welcome / onboarding-name / onboarding-notify / settings-subscription / meditation-pause: page composition — wrong balance. Chair-measured empty bands of 382px (welcome, y=345-726), 471px (name), 405px (notify), 225px (subscription) and 159px of dead ground below the last control on meditation-pause-light, while simultaneously first ink sits at y=0 on onboarding-name/focus and last ink at y=843 of 844 on onboarding-name, settings-subscription and memory-locked-paywall. Floaty in the middle, cramped at both edges, one screen at a time.", "settings-light SettingRow / onboarding-name-light input / composer textarea / Sheet / Skeleton / MemoryDetail.tsx:124: border-radius — default framework values left standing. Grep counts rounded-2xl x17, rounded-3xl x2, rounded-xl x2 (21 untouched Tailwind defaults) against the app's own rounded-field used exactly once (VoiceSheet.tsx:128), plus rounded-full x23 beside rounded-pill x27 — six rendered radii where four role-named tokens are declared.", "timeline-light-full / timeline-dark-full, Thu Sep 3 CompactCard: keepsake copy — machine tell. The closer "I'll take it, all of it." sits directly under its own title "Small win but i'll take it" because scripted.ts:92 builds bodyWords from 4+ letter words but echoes() only extracts 5+ letter words from the candidate, so "take" and "i'll" never register; compounded on the hero cards by the dropped user line and by lowercase i beside capitalised sentence starts at 18px."
- Regressions: none
- Verdict source: council
- Jury: Opus, Fable, Sonnet, Sonnet, Opus · confidence low (The User & Brand Advocate's peer review failed on a provider rate limit, so its scores stand unrevised while the other four were tested — and its untested A1=11 is the sole reason the A1 median is 9.5 rather than 9 (the three tested scores are 9, 9, 10); compounding that, B5 jumped 5 to 7 and B4 3.5 to 5 in one round with four of five advisors clustered at the ceiling on each.)
- Improvement vs previous round: +7.5

## Decision: CONTINUE

send the top 3 fixes to the builder (+7.5 vs last round)

## Top fixes for the next round (highest leverage first)

1. app/globals.css :root — declare --pt-safe-min: 1.5rem and --pb-safe-min: 1.5rem (near the --radius-* block at line 160). .pt-safe/.pb-safe currently compute to max(env(),0px)=0px and, sitting later in the same @layer utilities at equal specificity (.py-6 at byte 21741 vs .pt-safe at 34490 in the built CSS), erase the declared py-6/py-8 on all five onboarding steps, all six settings pages, /help and MemoryDetail. Target: onboarding-name-light first ink moves from y=0 to y>=24 and last ink from y=843 to y<=820. Verify in the built CSS, not the source — the globals.css:234 comment claims this was already fixed and it was not.
2. components/chat/Bubble.tsx:21 — replace the per-message <button type="button" title="tap for the time"> with a non-interactive element and move the timestamp reveal onto the message group's existing timestamp row. Target: thread drops from 34 audited interactive targets to under 10 and thread-crisis from 45 to under 12, so a screen reader reads a journal as a log, not a list of buttons. In the same pass move id="main" off app/(app)/layout.tsx:20's wrapper <div> onto the <main> inside each (app) route (Thread.tsx:28, TimelineView.tsx:85, settings/page.tsx:24), matching what StepShell.tsx:14 and help/page.tsx:14 already do correctly, so app/layout.tsx:66's skip link stops landing before the header it means to skip.
3. components/timeline/MemoryCard.tsx and MemoryDetail.tsx — render day.title (as the card headline in Fraunces 24 with the date demoted to a 14px caption, or folded into reflection ahead of the closer). Target: today's card on timeline-light-full reads "…and i didn't stop once. But i made it through, and i'm home now." instead of ending on "Not every day has to be more than survived." Then in lib/adapters/ai/scripted.ts widen echoes() from [a-z']{5,} to [a-z']{4,} (fixes the Thu Sep 3 echo), extend sentence() to uppercase the standalone pronoun (\bi\b plus i'm/i'll/i've/i'd), and lowercase every moodLabel so "Bright & warm" becomes "bright & warm".
