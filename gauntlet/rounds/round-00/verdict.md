# Round 0 — orchestrator verdict

- Tier A (machine): 16/40 · Tier B (council): 22.5/35 · Tier C (council): 14.5/25
- Raw total: **53** · after caps: **53**
- Caps applied: primary action <44px / outside thumb zone → 80; console error / dead end → 75
- Red flags: "meditation-pause (light + dark): page background — unmotivated full-bleed bg-gradient-to-b from-surface to-bg (BreathingPacer.tsx:55); the brand permits only sunlight→honey behind Pip, and this wash is the direct cause of the 39.2% dominant-colour failure in dark", "thread / settings / timeline: page spacing — default Tailwind half-steps left standing (12px x344, 6px x336, 10px x218 off-grid) plus four different page gutters (12/16/20/24px) and a lone p-7; chair-verified settings-light gutter x20 vs settings-subscription-light x55", "timeline-light-full + settings-light: card stacks — eleven white rounded boxes down the timeline, of which the EARLIER run is five identical CompactCards, then a paywall card; settings is five identical rows", "memory-locked-paywall (both themes) + timeline + meditation-pause: copy — 'Unlock with Pip+ ($4.99/mo)' and 'unlock your full timeline' use the banned verb; 'MINDFUL PAUSE' tracked uppercase, '0:01 / 1:30' and '4-4-4-4 Box · tap to switch' are spec voice; 'synthesized by pip with care' is a machine describing itself", "timeline-light-full: every memory card — mail-merge templating, all 7 reflections closing on 'Looking back, it felt like a lot to hold, and I got through it.' including the Bright and Calm days, each opening on the ungrammatical 'Today I ' + raw text", "settings-subscription-light + memory-locked-paywall (both) + memory-card-light: page bottoms — floaty balance, content ending at y=300 / y=400 / y=537 with 540px / 440px / 300px of empty ground below on an 844px viewport", "whole app: type ramp — 10 rendered sizes (11, 12, 14, 15, 16, 17, 18, 24, 30, 36px) against a ramp of six, with 15, 16, 17 and 18 all coexisting", "settings-dark: 'get help now' card — muddy colour, the blush warmth rendering as a brown-mauve slab of untokenised #584838 at 8.2% of pixels plus #A87878, with the light-mode yellow icon chips vanishing entirely"
- Regressions: none
- Improvement vs previous round: baseline

## Decision: FORCE-FIX

Tier A gate failing (A2, A3, A4, A5); no credit until the machine gates pass

## Top fixes for the next round (highest leverage first)

1. Lift the →75 cap: give the flex child in TimelineView.tsx's <main className="mx-auto max-w-2xl px-4 py-4"> min-w-0 w-full so timeline (light + dark) measures ≤390px at a 390px viewport with 0px horizontal overflow and reflections wrap instead of clipping mid-word, and catch MemoryDetail's expected 402 on /api/memory/:date so all 21 renders log 0 console errors
2. Lift the →80 cap: Bubble.tsx py-2.5 → py-3.5 takes every chat bubble from 40.6px to 48px and clears ~33 of the 67 undersized targets in one line; then the onboarding-rhythm time inputs (117.9x28 → ≥44px), the welcome 'already have pip? sign in' link (342x20 → ≥44px), and the single CTA on memory-locked-paywall / settings-subscription / help / timeline re-shelled with StepShell's bottom-anchored pattern — target 0/210 targets under 44x44 and 21/21 screens with a ≥44px primary on screen in the thumb zone
3. Make the memory a memory and give Pip more than one line: kill 'Today I ' + text.slice(0,80) and the fixed closing sentence in lib/adapters/ai/scripted.ts:60 so 0 of 7 cards share a last line and a Bright day never reads as 'a lot to hold'; widen the reply set at scripted.ts:33 so no two consecutive Pip bubbles repeat; raise pip-bubble #FFF3D1 off cream #FFF9ED with a hairline ring-1 ring-line; and replace both instances of 'unlock' on the paywall with a honey CTA plus a real 'not now'
