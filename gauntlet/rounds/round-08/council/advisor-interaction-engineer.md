# Interaction Engineer — round 08

## Scorecard
| Criterion | Score | Evidence |
|---|---|---|
| A1 | 11/12 | Gate: 0 axe violations, 0 contrast fails, 80/80 focus-visible, 0 running animations under reduced-motion. Source cross-check confirms two prior defects are now fixed: `TimelineView.tsx:116-131` mood filter is `role="radiogroup"`/`role="radio"`/`aria-checked` (correct pattern, not the old mislabeled tablist), and `BreathingPacer.tsx:68-76` no longer wraps the mutating `<h1>{state.phaseName}…</h1>` in `aria-live` — the live region is now a separate `sr-only` span (line 76) with a static `<p className="sr-only">{clock} of {total}</p>` clock alongside it. Docked one point for something the gate can't see: `VoiceSheet.tsx:91` puts `aria-live="polite"` on the paragraph rendering live speech-recognition `interim` results — while dictating, that node's text changes on every recognition tick, which will read as constant chatter to a screen-reader user on the one surface (voice capture) meant to be quick and quiet. |
| A2 | 8/8 | Gate: 0/123 targets <44px, primary CTA ≥44px and in the thumb zone on all 23 screens. Verified in code: `Composer.tsx` send/photo/mic buttons carry `.tap` (44×44 min, `globals.css:.tap`); `Button.tsx` sizes are h-11/h-12/h-14 (44/48/56px). Screens: onboarding CTAs measure 342×56 (gate), composer buttons 44×44 (gate). |
| A3 | 8/8 | Gate: 94.6% of spacing on the 8pt grid, remainder on the documented 4pt micro-step (149× 4px, 4× 12px — an explicit exception per the brief's "4pt micro-steps only inside components"), 0 misaligned siblings. |
| A4 | 6/6 | Gate: 6 sizes (12/14/16/18/24/32px), 2 families (inter, newsreader), 3 weights (400/600/700). `globals.css` wipes Tailwind's `--text-*` namespace to exactly six entries so an off-ramp size fails silently rather than sneaking in — verified by reading the token block. |
| A5 | 6/6 | Gate: 0 console errors, CLS ≤0.1 on 23/23 (thread-crisis measured 0.015), FCP ≤1800ms everywhere (36–196ms), 14/14 sticky chrome retained on scroll, 0 sideways scroll, 0 dead ends. Verified in code: `TopBar.tsx:6` is `sticky top-0 z-30`, `Composer.tsx:70` footer is `sticky bottom-0 z-20` — matches the gate's sticky finding. |
| B1 | 7/8 | One focal point per screen, deliberately sized: `thread` puts all visual weight on the message list under a 30px avatar + 18px wordmark top bar (`ThreadTopBar.tsx`); `timeline`/`memory-card` use the type ramp's top two sizes (32px `text-3xl` h1, 24px `text-2xl`/reading h2) so the date-vs-title fight the taste brief warns about doesn't happen — `MemoryDetail.tsx:98` makes the reflection title itself the h1, at 32px, with the date dropped to 14px (`text-sm`) beneath it. Docked one point: `memory-locked-paywall-light.jpg` still shows the blurred locked-day skeleton (date at 24px reading serif) directly above the paywall card's own 24px "keep your whole story" heading — two same-size serif blocks stacked, even though one is deliberately blurred/muted. |
| B2 | 7/7 | Single component vocabulary reused everywhere: `Button.tsx`, `Chip.tsx`, `Pill.tsx`, `Sheet.tsx`, `Skeleton.tsx`, `TopBar.tsx`, one `Icon.tsx` wrapper fixing `strokeWidth={1.75}` app-wide. Radius tokens (`--radius-field/bubble/card/pill`) are the only radii in use across chat bubbles, cards, sheets and pills — no one-off `rounded-[Xpx]` found in the files read. |
| B3 | 6/7 | Verified in source, not visible in any static capture (no screenshot shows a mid-press or mid-arrival state): `Button.tsx` — `transition-[filter,transform,background-color] duration-150 ease-soft active:scale-[0.98]`; `Chip.tsx`/toggle switches (`RhythmEditor.tsx:62-63`) — `transition-colors/transform duration-150`; `CompactCard.tsx:10` — `active:scale-[0.99] duration-150`. Every value in-band (150ms). Optimistic send confirmed in `threadStore.ts:64-81`: message renders instantly at `opacity-70` (`Bubble.tsx:29`) then flips to `status:"sent"`; failure shows "didn't send · tap to retry" (`Bubble.tsx:36-39`), a real self-confirming action, not a silent failure. Skeletons match their content's shape (`MemoryCard.tsx:37-38` two text-width `Skeleton` bars under "pip is wrapping up…", not a spinner). Pip's replies stagger with a length-scaled dwell (`grouping.ts:43`, `dwellMs = min(2200, 500+25*len)`) and the typing indicator toggles between bubbles (`threadStore.ts:167-176`) — a genuine "texting a friend" cadence. Docked one point: (a) none of this is confirmable from the 23 static jpgs — I'm trusting source entirely for press/stagger/retry; (b) the same `aria-live` interim-transcript issue flagged under A1 is also a micro-interaction defect (an action that "confirms itself" too loudly). |
| B4 | 5/5 | Keyframes read calm at every value: `pip-breathe` scales 1→1.035 over 4.2s, `pip-blink` is a 0.165s blink inside a 5.5s cycle, `glow` breathes opacity 0.55↔0.9 over 3.6s — nothing snaps or bounces gratuitously. `useBreathing.ts`/`engine.ts` drive the pacer's scale purely from elapsed time (no timers duplicating state), and `BreathingPacer.tsx` sets `--motion-scale:1.6` to slow the shared bubble/fade animations specifically on the one screen meant to slow the user down. Reduced-motion in `globals.css:271-286` kills all `@keyframes` via a blanket `*{animation-duration:.01ms!important}` rule, then carves out exactly one named exception, `.breath-eased` (higher specificity via its class selector beats the universal-selector `!important`, so the cascade math is sound), which eases the pacer's transform to 1.2s instead of switching it off — matching the brief's "ease way down… without the UI feeling dead." Gate confirms 0 animations running under reduced motion on every probed screen, including `meditation-pause`. |
| B5 | 7/8 | Copy is specific and lowercase throughout ("hey, i'm pip", "that sounds heavy.", "i made it through, and i'm home now") — no "unlock"/"journey"/"powerful" found in any component read. The only gradient in the codebase is Pip's own warm radial glow (`PipMascot.tsx:38-46`, sunlight→honey) plus the onboarding/thread illustration — matches the brand's single permitted gradient. `MemoryDetail.tsx:52-62` uses `blur-[3px] saturate-50` on the locked day purposefully (show the shape, not the words) rather than decorative glass. Docked one point: the mountain/sun illustration is identical across every thread/composing/crisis screenshot (`thread-light/dark`, `thread-composing-photo-light/dark`) — it's the single image asset, not a filler grid, but it is the one repeated visual motif in the build. |
| C1 | 6/7 | One decision per onboarding step (`StepShell.tsx` centers a single question/action group); composer has exactly one text field + one contextual action (photo or send/mic, `Composer.tsx:127-146`); settings groups are labeled and short (`settings-light.jpg`: 3 groups of 2). Docked one point: `settings-light.jpg`'s "get help now" banner sits above all settings rows, competing for first-glance attention with the "you" section immediately below it. |
| C2 | 7/7 | Copy tracks the brand doc's own examples almost verbatim: thread shows "that sounds heavy." and "you don't have to sort it out right now. what's the part sitting with you most?" against the brief's own "that sounds like a lot to carry today." Crisis flow (`thread-crisis-light.jpg`, `CrisisCard.tsx`) is calm and honest — "i'll stay here while you reach them" — with 988/Crisis Text Line/Samaritans/Lifeline rows, matching the spec's region-appropriate resource list exactly. |
| C3 | 6/6 | `keepsake-in` (0.3s spring) is reserved for the just-synthesized "today" card only (`MemoryCard.tsx:16`, `fresh = i===0 && d.date===today`) — every other card gets the plainer `fade-up`; the one milestone copy line, "a whole week, kept," appears only when `reflectionCount >= dots.length` (`WeekFlow.tsx:15,38-42`), never as a permanent badge. Both moments are conditional and specific, not decoration running by default. |
| C4 | 5/5 | Send is one tap once text exists (`Composer.tsx`), matching the "two taps to capture" spec. `StepShell.tsx`'s progress bar is an endowed-progress design (`(step+1)/(total+1)`, so the welcome screen counts as done) — a documented, deliberate onboarding-speed device. Paywall (`PaywallCard.tsx`) states price plainly, offers "not right now" and "cancel anytime · export and delete stay free, always" in the same breath as the CTA — no forced continue, no dark pattern. |

## Red flags
- None found. Checked: gradients (only the single brand-sanctioned sunlight→honey glow, `PipMascot.tsx`; no purple/blue or multi-hue gradients anywhere read), default Tailwind spacing (A3 gate: 94.6% on 8pt, remainder on documented 4pt sub-grid, not stray defaults), color/font count (A4 gate: 2 families/6 sizes/3 weights), repeated card-grid filler (timeline mixes `MemoryCard`/`CompactCard`, no identical stacked tiles), generic SaaS copy (none in any component's literal strings), glassmorphism/blur-for-its-own-sake (the one blur use is a deliberate locked-content preview, not decoration), motion-because-possible (every keyframe ties to a specific state: send, arrival, breath, milestone — none idle-decorative beyond Pip's own breathing/blink, which the brand doc explicitly asks for).

## Top 3 fixes (highest leverage first)
1. `components/pause/BreathingPacer.tsx` and `components/chat/VoiceSheet.tsx`: the pacer's phase-name live region is already fixed and isolated — do the same audit on `VoiceSheet.tsx:91`. Its `aria-live="polite"` paragraph re-renders on every `interim` speech-recognition tick; move the live announcement to only fire on `finalText` commits (or drop `aria-live` and rely on the visible caption + the explicit "done" button), so voice capture doesn't talk over itself for screen-reader users the way the pause screen used to.
2. `components/timeline/MemoryDetail.tsx:52-62` / `memory-locked-paywall-light.jpg`: the locked-day skeleton's 24px reading-serif date sits directly above the paywall card's own 24px heading. Drop the locked skeleton's date size a step (e.g. 18px) so only one 24px+ block competes for the eye on that screen.
3. Nothing in the shipped code contradicts the brand doc, but it's worth a peer check: `PipAvatar.tsx:6` hardcodes `idle={false}` for every avatar instance (thread header, message groups, typing indicator), so Pip never breathes or blinks in the thread itself — only in onboarding and the pause screen. This matches brand-guidelines' avatar/animated-presence split, but not the taste-brief's specific "breathing idle Pip in the thread header" note; flag for the User & Brand Advocate to weigh which document should win here.

## Notes for peers
- Ergonomics Auditor: please verify my A1 VoiceSheet finding independently — I read it from `VoiceSheet.tsx:91` only, have not run a screen reader against it.
- Taste Critic: the single mountain/sun illustration repeats across every thread-family screenshot; worth deciding whether that's restraint or repetition for B5.
- User & Brand Advocate: see the PipAvatar `idle=false` note above — a genuine two-document disagreement, not a bug.
- All B3/B4 timing values (150ms/240ms/260ms/300ms/4.2s/5.5s/3.6s) are read from `globals.css` and component `className`/inline-style strings; none are confirmable from the 23 static jpgs, which only show rest-states. Treat my B3/B4 evidence as source-verified, not screenshot-verified.

```json
{"persona":"Interaction Engineer","scores":{"A1":11,"A2":8,"A3":8,"A4":6,"A5":6,"B1":7,"B2":7,"B3":6,"B4":5,"B5":7,"C1":6,"C2":7,"C3":6,"C4":5},"redFlags":[],"topFixes":["VoiceSheet.tsx:91 — aria-live=polite fires on every interim speech-recognition tick; fire it only on finalText commits","MemoryDetail.tsx locked-day skeleton date (24px) competes with the paywall card's own 24px heading directly below it on memory-locked-paywall","PipAvatar.tsx hardcodes idle=false everywhere — thread-header Pip never breathes/blinks, which follows brand-guidelines' avatar/animated-presence split but not the taste-brief's thread-header breathing note; needs a brand-vs-brief call, not a code fix"]}
```
