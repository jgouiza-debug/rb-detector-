# Round 6 — orchestrator verdict

- Tier A (machine): 40/40 · Tier B (council): 29/35 · Tier C (council): 20/25
- Raw total: **89** · after caps: **89** · effective (regression, zero credit): **89**
- Caps applied: none
- Red flags: "onboarding-name-light / -welcome-light / -notify-light: copy block to anchored CTA — ~430px of untouched cream between the input's lower edge (y~358) and the continue button (y~790), over half an 844px screen, on screens with no keyboard to justify StepShell's justify-start", "settings-light / settings-dark: HelpNowCard vs SettingRow — the radius namespace was never wiped the way --text-* was, so Tailwind defaults ship alongside the ladder (rounded-2xl x17, rounded-3xl x2, rounded-xl x2, rounded-t x1) and a 20px card with a 40px chip sits directly above 16px rows with 36px chips", "VoiceSheet listening state: animate-voicebar — 7 bars staggered 90ms read as a live mic waveform while hooks/useDictation.ts contains no AnalyserNode, no getUserMedia and no level analysis, so the motion can never reflect input (VoiceSheet.tsx:86-90)", "timeline-dark: mood chips — lib/theme/tokens.ts:42-51 is theme-agnostic, so the mixed chip renders #FFF3D1 on a #26231F card and is the brightest chip on timeline-dark-full.jpg, above bright (#FFDE7A); the parallel claim about heavy (#D6CCBC) was refuted on measured luminance (0.804 vs 0.870)", "memory-card light + dark: the keepsake title and reflection — title is the day's last entry verbatim and the reflection is the earlier entries verbatim plus one canned closing, both reprinted 200px below under WHAT I SAID, THAT DAY (scripted.ts:119-131, confirmed on memory-card-light-full.jpg)", "thread / thread-composing-photo / timeline-*-full / memory-card-*: the day's photo — tests/e2e/fixtures/photo.jpg is a 400x300 solid #F9C97C JPEG seeded as real content, so every photograph renders as a flat orange block, full-bleed at y~80-245 on thread-light.jpg and twice on memory-card-light-full.jpg", "timeline mood filter: role=tablist/role=tab with aria-selected but no aria-controls, no tabpanel, no roving tabIndex and no onKeyDown anywhere in the file (TimelineView.tsx:116-131), while the house Chip component correctly uses role=checkbox/aria-checked", "meditation-pause: the phase heading — aria-live=polite wraps the mutating <h1> and its copy line (BreathingPacer.tsx:64-66) while BOX cycles 4/4/4/4s (patterns.ts:20-24), forcing ~22 double screen-reader announcements in a 90s session on the calmest screen in the app", "settings-subscription: 'just paid? tap here' — rendered outside the plus ternary (SubscriptionActions.tsx:47-48), so a user who has never paid is offered a webhook-recovery button as a second same-width pill under the primary CTA, though pip_checkout_pending is already written to localStorage at checkout", "memory-locked-paywall: the blurred ghost card — the comment at MemoryDetail.tsx:49-50 says it shows 'the length of what you wrote', but the body is five hard-coded widths identical for every day; the blur itself is cleared as motivated, the fabricated content beneath it is not"
- Regressions: B1, B4
- Verdict source: council
- Jury: Opus, Fable, Sonnet, Sonnet, Opus · confidence low (The 87-92 spread was the tightest of any round and does not survive inspection: three advisors gave A1 a clean 12/12 while all three missed the same two ARIA defects a fourth found in source, three gave A4 6/6 on a font-role leak the round-5 chair had already written down, the advisor totalling 92 self-voided four of its own criteria in peer review, and the build's most consequential defect (the keepsake reprinting the user's own sentences) was found by one advisor of five and understated even there — convergence on the shared gate report, with a correlated blind spot everywhere the gate is silent.)
- Improvement vs previous round: -2

## Decision: FORCE-FIX

regression on B1, B4: this round scores zero improvement; fix it before anything else

## Top fixes for the next round (highest leverage first)

1. memory-card, the <h1> title and the reflection <p>: in lib/adapters/ai/scripted.ts:119-131 the title is the last entry and the reflection is the earlier entries, both only recapitalised, while MemoryDetail.tsx:123-131 lists those same entries below. Target: no sentence in the title or reflection may appear verbatim (case-insensitive, punctuation-stripped) in the entries under 'what i said, that day' — either synthesise across the day per design-spec 4.4 or collapse the source entries behind a 'see what i actually wrote' disclosure. Same commit: replace tests/e2e/fixtures/photo.jpg (400x300 solid #F9C97C) with a real photograph, which repairs six renders.
2. timeline mood filter and meditation-pause phase heading: TimelineView.tsx:116-131 — replace role=tablist/role=tab with role=radiogroup/role=radio + aria-checked (the existing Chip pattern, which is what a single-select filter is), target zero role=tab without aria-controls, roving tabIndex and arrow keys. BreathingPacer.tsx:64-66 — move aria-live=polite off the mutating <h1> onto a separate sr-only node, target at most 1 announcement per breath cycle rather than 2 per phase.
3. meditation-pause, three edits on one screen: BreathingPacer.tsx:65 font-reading -> font-display, target zero font-reading outside components/timeline/*; demote the 'i feel ready' Button (full, size lg, 320x56, #F5B841) to variant=soft and drop PatternToggle to a text link, so the 240px ring and the 120px cozy Pip are the loudest objects; centre the stack to close the ~167px of empty cream below 'pause'.
